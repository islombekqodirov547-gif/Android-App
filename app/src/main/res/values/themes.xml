<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. UI is driven by Compose; this theme only sets
         the window background, status bar and splash screen appearance. -->
    <style name="Theme.JeskoSavdo" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.JeskoSavdo.Main</item>
    </style>

    <style name="Theme.JeskoSavdo.Main" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
